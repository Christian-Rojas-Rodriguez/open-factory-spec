# Workflow — [Your Project]

> This file is **derived from your approved RFC** by the `plan-workflow` skill. Run `/factory-init "[your project description]"` to generate it through the two-gate Bootstrap process.
>
> **Do not edit manually** — it is the sovereign declarant of your project's agents, skills, hooks, and commands at runtime. To add a new component, run `/agent-new` or `/spec-new` first.

---

Run `/factory-init "[your project description]"` in Claude Code to bootstrap your project. This will:

1. Conduct a product interview (PRD) and a technical interview (RFC) with you.
2. Derive `workflow.md`, `constitution.md`, and all task skeletons from your approved RFC.
3. Leave you ready to run `/task-run <id>` to implement your first task.
